<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme - Force light mode regardless of system setting -->
    <style name="Base.Theme.SmartQuit" parent="Theme.Material3.Light.NoActionBar">
        <!-- Force white background -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:colorBackground">@android:color/white</item>
    </style>

    <style name="Theme.SmartQuit" parent="Base.Theme.SmartQuit" />
</resources>